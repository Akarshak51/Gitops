# DO NOT commit real values. Apply this once manually (kubectl apply -f secrets.yaml)
# or generate it with `kubectl create secret generic ...` and keep this file as a
# reference/template only (values below are base64 of placeholders).
apiVersion: v1
kind: Secret
metadata:
  name: ai-task-secrets
  namespace: ai-task-platform
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_A_LONG_RANDOM_SECRET"
  MONGO_ROOT_PASSWORD: "REPLACE_WITH_STRONG_PASSWORD"
